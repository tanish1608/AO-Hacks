import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const experiments=sqliteTable('experiments',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull(),name:text('name').notNull(),status:text('status').notNull(),revision:integer('revision').notNull().default(0),payload:text('payload').notNull(),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull(),leaseToken:text('lease_token'),leaseUntil:integer('lease_until')},t=>[index('idx_experiments_owner_created').on(t.ownerId,t.createdAt)]);
export const releases=sqliteTable('releases',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull(),experimentId:text('experiment_id').notNull().references(()=>experiments.id),architectureDigest:text('architecture_digest').notNull(),payload:text('payload').notNull(),createdAt:text('created_at').notNull()},t=>[index('idx_releases_owner_created').on(t.ownerId,t.createdAt),uniqueIndex('idx_releases_owner_experiment').on(t.ownerId,t.experimentId)]);
